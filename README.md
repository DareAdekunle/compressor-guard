# compressor-guard
Predictive maintenance for industrial air compressors on real sensor data (MetroPT-3). Unsupervised anomaly detection (Isolation Forest, LSTM autoencoder) back-tested against documented failures, with a maintenance-economics model for alert thresholds. FastAPI + Streamlit demo.
